#!/bin/bash 

cmd="$1"
if [ ! -z $cmd ] 
then
    obix-install "$@"
fi

install-obix() {
    cd ~
    git clone https://github.com/oberon-git/obix
    cd obix
    ./install
    cd ../
    rm -rf obix
}

install-obix > /dev/null 2>&1
